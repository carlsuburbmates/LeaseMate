# pnpm settings (pnpm v10+ reads these from .npmrc / pnpm-workspace.yaml, not package.json)
# See: https://pnpm.io/settings

# Patch: wouter@3.7.1 uses a local patch file
# (pnpm v10 reads patchedDependencies from pnpm-workspace.yaml or .npmrc)
